import React from 'react'
import { SafeAreaView, Text, StyleSheet } from 'react-native'

const TestScreen = () => {
  return (
    <SafeAreaView>
      <Text>Test screen - i väntan på dashboard</Text>
    </SafeAreaView>
  )
}

export default TestScreen

const styles = StyleSheet.create({})
